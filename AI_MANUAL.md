# DriverFund 修正マニュアル（AIに任せる版）

> **プログラミングの知識は要りません。**
> やることは「AIに日本語でお願いする」だけです。
>
> このマニュアルは **Codex（ChatGPT）を使う前提**で書いています。
> インストール作業は不要です。ブラウザだけで完結します。

---

## 0. 3行でわかる仕組み

```
あなた「〇〇を直して」
      ↓
Codex がコードを直して「変更案」を出す
      ↓
あなたが内容を見て「Merge（反映）」ボタンを押す
      ↓
1〜2分後、自動でサイトに反映される
```

**Vercel（サイトを配信している場所）は触りません。** 自動です。

> Codexは勝手に本番へ反映しません。**必ずあなたの承認（Merge）が要ります。**
> なので気軽に頼んで大丈夫です。気に入らなければ捨てられます。

---

## 1. 最初の1回だけやる準備（5分・インストール不要）

### 1-1. GitHubアカウントを作る

https://github.com で無料アカウントを作ります。
作ったら**アカウント名を戸田に伝えてください。** アプリ一式を移管します。

### 1-2. CodexとGitHubを繋ぐ

1. https://chatgpt.com/codex を開く
2. GitHubアカウントで連携を許可
3. リポジトリ一覧から **`DriverFund`** を選ぶ

これで準備完了です。以降、ブラウザだけで修正できます。

> **「GitHubのURLを渡す」必要はありません。**
> 一度リポジトリを繋げば、Codexが中身を全部読める状態になります。
> 毎回URLを貼る必要はなく、リポジトリを選んで話しかけるだけです。

---

## 2. 修正のやり方（ここが本番）

https://chatgpt.com/codex を開き、リポジトリ `DriverFund` を選んで、
**日本語で頼むだけ**です。

### そのまま使えるプロンプト集

**■ 文言を変えたい**
```
トップページの「あなたへのおすすめ」という見出しを
「注目のドライバー」に変えてください。
```

**■ 色を変えたい**
```
アプリのメインカラーを今の赤から濃い青に変えたいです。
lib/theme.ts のトークンを変更してください。
色はハードコードせず、必ずトークン経由でお願いします。
```

**■ カテゴリを追加したい**
```
レースカテゴリに「GT」と「フォーミュラ」を追加してください。
CLAUDE.md にカテゴリ変更時の更新箇所リストがあるので、
それに従って必要な箇所を全部漏れなく直してください。
```

**■ 画面の見た目を変えたい**
```
ドライバー登録画面（app/(driver-onboard)/welcome.tsx）の
メリット紹介部分が文字ばかりで単調です。
もっと視覚的に魅力が伝わるデザインにしてください。
lib/theme.ts のデザイントークンを使ってください。
```

**■ 動かない・エラーが出た**
```
アプリで〇〇をしたら△△というエラーが出ました。
原因を調べて直してください。
推測で直さず、実際にコードを読んで確認してから直してください。
```

### 最初に必ず言っておくと良いこと

新しく作業を始めるときは、最初にこう言っておくと精度が上がります。

```
まず CLAUDE.md と AI_MANUAL.md を読んで、
このプロジェクトのルールを把握してから作業してください。
```

### 変更を本番サイトに反映する手順

Codexは作業が終わると **「Create PR」（変更案を作る）** を提案します。

1. Codexの画面で **Create PR** を押す
2. GitHubに移動し、変更内容が表示される
3. 内容を確認して **「Merge pull request」** を押す
4. **1〜2分後、自動で本番サイトに反映されます**

> **4は自動です。** Vercelは触りません。
> https://driverfund-app.vercel.app を再読み込みすれば反映されています。

> **変更をやめたいとき**は、Mergeせず「Close pull request」で捨てられます。
> 本番には一切影響しません。

---

## 3. AIに渡すURL・情報一覧

AIが「〇〇の情報が必要」と言ったときは、ここから渡してください。

| 何 | URL | 用途 |
|---|---|---|
| **コード置き場** | https://github.com/（移管後のアカウント）/DriverFund | AIが直す対象 |
| **公開サイト** | https://driverfund-app.vercel.app | 反映結果の確認 |
| **データベース** | https://supabase.com/dashboard | 選手データ・ログイン管理 |
| **配信サーバー** | https://vercel.com/dashboard | 基本触らない |
| **決済** | https://dashboard.stripe.com | 入金確認 |

### 選手データを見る・直したいとき

1. https://supabase.com/dashboard にログイン
2. 組織「DriverFund」→ プロジェクト `driverfund` を開く
3. 左メニュー **Table Editor** → `drivers` テーブル
   - 選手のプロフィールがここにあります
   - `is_published` を `true` にするとサイトに公開されます
4. ログインユーザーを見るには **Authentication → Users**

> 表計算ソフトのように直接編集できますが、**削除は慎重に。**
> 迷ったらCodexに「Supabaseの〇〇を変えたい」と相談してください。

### APIキーが必要になったとき

Codexが「Supabaseのキーを教えて」と言うことは通常ありません。
もし必要になったら Supabaseダッシュボード → **Settings（歯車）→ API** から
`Project URL` と `anon` `public` キーをコピーします。

> ⚠️ `service_role` と書かれたキーは**絶対に渡さない**でください。
> 全権限を持つ鍵で、漏れると誰でもデータを消せます。

---

## 4. やってはいけないこと

| ❌ | なぜ |
|---|---|
| `service_role` キーを人やAIに渡す | 全権限の鍵です。漏れると誰でもデータを消せます |
| APIキーをSNSやチャットに貼る | 同上。Codexに渡す必要も基本ありません |
| Supabaseのテーブルを勢いで削除 | アプリが動かなくなります。先にCodexに相談を |
| 中身を確認せずPRをMergeする | 壊れたまま本番に出ます。ざっとでいいので目を通す |
| Supabaseプロジェクトを1週間以上放置 | 無料枠は自動停止します（→ 5章） |

---

## 5. 知っておくべき「詰まりポイント」

### ■ Supabaseの自動停止
**無料プランは1週間アクセスがないと自動停止します。**
停止するとアプリが全部動かなくなります（登録・ログイン・決済すべて）。

- 復旧: ダッシュボードで「Resume project」を押すだけ
- データは消えません（停止から**1年以内**なら復元可能）
- 恒久対策: 有料プラン（月25ドル程度）にすると停止しません

> 2026年8月、実際にこれで2ヶ月間サービスが停止していました。

### ■ Vercelの規約
Vercelの無料プラン（Hobby）は**非商用の個人利用限定**です。
DriverFundは決済を扱うため、**本格的に収益化する際は
Proプラン（月20ドル/人）への変更が必要**です。

### ■ 環境変数を変えたら再デプロイ
Vercelの環境変数を変更しても、**再デプロイしないと反映されません。**
「キーを変えたのに直らない」の原因はほぼこれです。

---

## 6. 困ったときのプロンプト

**■ 仕組みを知りたい**
```
このリポジトリの AI_MANUAL.md と HANDOVER.md を読んで、
〇〇について初心者にも分かるように説明してください。
```

**■ アプリが動かない**
```
アプリが動きません。何が原因か調査してください。
Supabaseが自動停止している可能性が高いので、まずそこを疑ってください。
```
→ Supabaseの停止が原因なら、ダッシュボードで
　 **Resume project** を押せば復活します（→ 5章）

**■ 自分が何をしたか分からなくなった**
```
直近の変更内容を、専門用語を使わずに説明してください。
```

---

## 7. 技術的な詳細（AI向け・人は読まなくていい）

- **フレームワーク**: Expo Router (React Native Web) → Web書き出し
- **DB/認証/ストレージ**: Supabase (PostgreSQL + RLS)
- **決済**: Stripe (Payment Links + Connect) / Supabase Edge Functions 経由
- **ホスティング**: Vercel（`expo export -p web` でビルド → `dist/` を配信）
- **デザイン**: `lib/theme.ts` のトークンを唯一の情報源とする。色のハードコード禁止
- **DBスキーマ**: `supabase/migrations/` に番号順。`0001` がベーステーブル
- **注意**: `supabase/functions/` は Deno 製のため `tsc` でエラーが出るが正常
